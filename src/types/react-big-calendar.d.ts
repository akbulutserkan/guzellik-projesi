declare module 'react-big-calendar' {
  import { Calendar, CalendarProps, View, Navigate, Event, SlotInfo } from 'react-big-calendar'
  export { Calendar, CalendarProps, View, Navigate, Event, SlotInfo }
  export * from 'react-big-calendar'
}