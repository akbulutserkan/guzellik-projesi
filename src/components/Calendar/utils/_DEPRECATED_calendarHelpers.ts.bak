'use client';

/**
 * @deprecated Bu dosya artık kullanımdan kaldırılmıştır.
 * Lütfen bunun yerine '@/utils/calendar/formatters' kullanın.
 * 
 * Merkezi ve dengeli mimari yapısında:
 * - Formatlama işlemleri: /utils/calendar/formatters.ts
 * - Veri çekme: /services/calendarService.ts
 * - State yönetimi: /hooks/useCalendarManagement.ts
 */

import {
  formatEventStyle,
  formatDayProps,
  formatSlotProps,
  calculateBusinessHours,
  formatTimeRange,
  convertWorkingHoursFormat,
  DAY_NAMES,
  determineTimeSlotType,
  getTimeSlotStyleByType,
  TimeSlotType,
  BusinessHours,
  BusinessHourSetting
} from '@/utils/calendar/formatters';

// Geriye dönük uyumluluk için tüm fonksiyonları ve tipleri re-export et
export {
  formatEventStyle,
  formatDayProps,
  formatSlotProps,
  calculateBusinessHours,
  formatTimeRange,
  convertWorkingHoursFormat,
  DAY_NAMES,
  determineTimeSlotType,
  getTimeSlotStyleByType,
  TimeSlotType,
  type BusinessHours,
  type BusinessHourSetting
};

// İlk kullanımda konsola uyarı yazdır
console.warn(
  '[Deprecated] calendarHelpers.ts kullanımdan kaldırılmıştır. ' +
  'Lütfen bunun yerine @/utils/calendar/formatters dosyasını kullanın.'
);
